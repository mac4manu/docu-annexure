## Packages
react-markdown | Rendering the converted markdown document content
react-dropzone | easy drag and drop file uploads
framer-motion | Smooth animations for UI transitions
date-fns | Formatting timestamps (e.g., "2 hours ago")

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
